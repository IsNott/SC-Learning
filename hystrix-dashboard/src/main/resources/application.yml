server:
  port: 8501
spring:
  application:
    name: Hystrix-dashborad
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8001/eureka/
#hystrix:
#  dashboard:
#    proxy-steam-allow-list: "localhost"